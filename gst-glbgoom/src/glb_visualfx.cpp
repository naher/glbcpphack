
#include "glb_visualfx.h"

#include "glb_vlines_visualfx.h"

namespace glb_goom {
	/*
template <class concrete_type, class data_type>
void GlbVisualFX<concrete_type, data_type>::init(PluginInfo *info) 
{ 
	static_cast<concrete_type*>(this)->v_init(info); 
}

template <class concrete_type, class data_type>
void GlbVisualFX<concrete_type, data_type>::free() 
{ 
	static_cast<concrete_type*>(this)->v_free(); 
}

template <class concrete_type, class data_type>
void GlbVisualFX<concrete_type, data_type>::apply(Pixel *src, Pixel *dest, PluginInfo *info) 
{  
	static_cast<concrete_type*>(this)->v_apply(src, dest, info); 
}

// template instantiation

template class GlbVisualFX<GlbVLinesVisualFX, GlbVLinesVisualFXData>;
*/
}